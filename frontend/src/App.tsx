import React from 'react';

export default function App() {
  return (
    <div style={{
      minHeight: '100vh',
      background: '#1a1a2e',
      color: 'white',
      display: 'grid',
      placeItems: 'center',
      fontFamily: 'system-ui, -apple-system, Segoe UI, Roboto, sans-serif'
    }}>
      <div>
        <h1 style={{textAlign:'center'}}>DataClean AI</h1>
        <p style={{opacity:.8, textAlign:'center'}}>Build OK ✅ — UI prête à être branchée</p>
      </div>
    </div>
  );
}
