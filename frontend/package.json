{
  "name": "dataclean-agent",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Static build via Vite copy' && vite build --emptyOutDir",
    "dev": "vite",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@vitejs/plugin-legacy": "^5.4.1",
    "vite": "^5.3.0"
  },
  "engines": {
    "node": ">=18"
  }
}
